#!/usr/bin/env python3
"""그림이 함께 쓰는 색.

**순서가 있는 변수에 한 색의 농담을 쓰지 않는다.** 경도와 두께는 순서가 있으므로
원칙적으로는 농담이 맞다. 그런데 이 캠페인의 그림은 군마다 유닛 곡선을 흐리게 깔거나
점을 찍으므로, 농담 셋이 서로 묻혀 **어느 군인지 못 가리는** 일이 실제로 일어났다
(2026-09-13, `optical_by_group`). 순서는 범례의 차례가 지고 색은 구별에 쓴다.

Okabe-Ito 계열 셋. 팔레트 검증기로 전 항목 통과를 확인했다 — 인접 쌍 최악
ΔE 11.0 (deutan), 보통 시야 25.8, 바탕 대비 전부 3:1 이상.
"""

CAT3 = ["#0072B2", "#D55E00", "#009E73"]        # 파랑 · 주황 · 초록

HARD3 = dict(zip(["soft", "medium", "hard"], CAT3))
THICK3 = dict(zip([1, 2, 3], CAT3))

# 원리는 서로 순서가 없으므로 원래부터 범주형이었다. 그대로 둔다.
PRINCIPLE = {"9DTact": "#1f6f8b", "DIGIT": "#c2553a", "DIGIT_Marker": "#6a8e3b"}
