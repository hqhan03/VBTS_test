# 시험 3 — 공간 분해능 (spatial resolution)

2026-09-05 ~ 09-07 측정, 09-07 정리. 9DTact 17 유닛 × 4 간격 × 3 깊이 = **204 측정점**.
데이터: `data/20260911_VBTSresolution_dataset/9DTact/20260905_passA_pair{100,075,050,025}/<sensor>/`,
판정: `scripts/analyse_resolution.py`, 결과표: `data/20260911_VBTSresolution_dataset/9DTact/resolution_measurements.csv`.

---

## 1. 무엇을 재는가

**두 점을 하나로 보는가, 둘로 보는가.** 지름 1 mm 기둥 두 개가 나란히 선 프로브
(`pair100` … `pair025`)를 젤에 눌러, 이미지에서 두 자국이 갈라져 보이는 가장 좁은
중심간격을 찾는다. DIGIT 360(arXiv:2411.02479)이 쓴 dual-pronged microindenter
방식을 따랐고, 판정도 그들의 MTF 기준을 함께 계산한다.

| 프로브 | 기둥 사이 틈 | **중심간격** | 공간주파수 |
|---|---|---|---|
| pair100 | 1.00 mm | **2.00 mm** | 0.25 lp/mm |
| pair075 | 0.75 mm | **1.75 mm** | 0.29 lp/mm |
| pair050 | 0.50 mm | **1.50 mm** | 0.33 lp/mm |
| pair025 | 0.25 mm | **1.25 mm** | 0.40 lp/mm |

네 프로브는 기둥 지름과 접촉 면적이 같고 간격만 다르다. 그래서 같은 깊이에서 자국의
진하기는 프로브 사이에 같아야 하고, 실제로 그렇다 — 이 사실이 §6의 재측정 판단 근거다.

---

## 2. 측정 절차

센서 하나를 장착하고, 프로브 네 개를 차례로 갈아 끼우며 각각 아래를 반복한다
(`scripts/pass_a_sensor.sh <sensor> <probe>`, 유닛당 약 2분).

1. **정렬** — 팁을 센서 축 위에, 프로브 축을 그 유닛의 **실측 젤 법선**에 맞춘다.
   17 유닛 모두 명목 법선에서 sensor y 로 +2.19° ± 0.65 기울어 있어(전부 같은 부호)
   명목 법선에 맞추면 기둥 두 개가 같은 깊이로 들어가지 않는다.
2. **영점** — 힘–깊이 곡선을 평면 압자 법칙(F ∝ δ¹)으로 맞춰 표면을 정한다. 기둥이
   1 mm 라 부러지기 쉬워 **첫 접촉에서 0.5 mm 까지만** 들어간다(운용자 한계). 접근
   시작점은 기록된 표면 + 2.0 mm: pair 프로브 팁이 4 mm 프로브보다 **약 1.10 mm
   길어서**(10 유닛 이상 실측, sd 0.13) 여백 1.0 mm 로는 시작점이 이미 젤 속이었다.
3. **깊이 사다리** — 0.1 / 0.2 / 0.3 mm, 각 1회. 깊이는 명령값이 아니라 로봇이 읽은
   값에서 영점을 뺀 것이다. 각 깊이에서 정지 후 프레임 한 장.
4. **후퇴** — 어떤 경로로 끝나든 젤 위 78 mm 로 올린다.

이미지 축척(mm/px)은 pair 패스에서 재지 않는다 — 열 곳을 눌러야 하는 축척 측정이 1 mm
기둥에는 과한 부하라서다. 축척은 센서의 광학에 속하므로 같은 유닛의 ball4 패스 값을
빌린다(§7 참조).

---

## 3. 판정 방법

각 프레임에서 기준 이미지와의 차이를 만들고, 두 자국의 중심을 잇는 축을 따라 밝기
프로파일을 뽑는다. 봉우리 두 개(P₁, P₂)와 그 사이 골(T)에서:

```
dip  = (P − T) / P,     P = min(P₁, P₂)        ← Rayleigh 계열
MTF  = (P − T) / (P + T)                          ← DIGIT 360 (Michelson)
sym  = min(P₁, P₂) / max(P₁, P₂)                 ← 두 기둥의 접촉 대칭성
```

**약한 봉우리 기준으로 판정한다.** 두 점이 "분해됐다"는 건 약한 쪽도 봉우리로 서
있다는 뜻이다. 평균을 쓰면 한쪽만 깊이 박힌 자국이 분해된 것처럼 보인다. 약한 쪽
기준은 판정을 엄격하게만 만든다.

세 관문을 다 넘어야 **분해**다:

| 관문 | 기준 | 근거 |
|---|---|---|
| 신호 | P ≥ **2.5** 회색조 | 시험 2에서 28 회색조가 0.548 mm 깊이에 해당(0.0196 mm/level). 깊이 영점의 σ 0.01–0.03 mm 는 1–1.5 level 이므로 2.5 level 은 영점 불확실도의 약 2σ. 이보다 약한 자국은 봉우리가 아니라 잡음이다 → **신호부족** |
| 잡음 | dip ≥ 3 σ_dip | 프로파일의 연속 차분으로 잡음을 추정(median\|Δ\|/0.6745/√2)해 dip 에 전파. 이보다 얕은 골은 잡음이 만든 것 → **잡음** |
| 깊이 | dip ≥ **0.265** | Rayleigh 기준(두 Airy 봉우리가 한 봉우리의 첫 영점에서 만날 때 골이 26.5 %) → 미달이면 **미분해** |

DIGIT 360 의 기준 MTF ≥ 0.5 는 dip 으로 환산하면 M = D/(2−D) 에서 **dip 0.667** —
Rayleigh 보다 **2.5배 엄격**하다. 둘 다 계산하고 둘 다 보고한다.

한 센서가 어떤 간격을 "분해한다"는 것은 세 깊이 중 **하나라도** 분해 판정인 것으로
정의했다.

---

## 4. 결과

### 4.1 간격별

| 중심간격 | 분해 센서 | dip 중앙값 | 약한 봉우리 중앙값 | MTF 중앙값 |
|---|---|---|---|---|
| **1.10 mm** | **2 / 8** ᵃ | 0.075 | — | — |
| 1.25 mm | **6 / 17** | 0.208 | 3.33 | 0.116 |
| 1.50 mm | **4 / 17** | 0.230 | 2.38 | 0.130 |
| 1.75 mm | **9 / 17** | 0.289 | 2.87 | 0.169 |
| 2.00 mm | **11 / 17** | 0.384 | 2.34 | 0.237 |

ᵃ `pair010` 은 2026-09-11 에 8 유닛에만 돌렸다 — 1.25 mm 를 이미 분해해 한계가 미결이던
유닛들이다. 나머지 9 유닛은 1.25 도 분해하지 못하므로 더 좁은 간격의 답이 정해져 있다.
자세한 것은 §4.5.

(아래 옛 표는 참고용으로 남긴다.)

| 중심간격 | 분해 센서 | dip 중앙값 | 약한 봉우리 중앙값 | MTF 중앙값 |
|---|---|---|---|---|
| 2.00 mm | **11 / 17** | 0.384 | 2.34 | 0.237 |
| 1.75 mm | **9 / 17** | 0.289 | 2.87 | 0.169 |
| 1.50 mm | **4 / 17** | 0.230 | 2.38 | 0.130 |
| 1.25 mm | **6 / 17** | 0.208 | 3.33 | 0.116 |

**9DTact 의 분해능 한계는 0.3 mm 이하 깊이에서 1.75 mm 와 1.50 mm 사이에 있다.**
2.00 → 1.75 mm 에서 dip 중앙값이 0.41 → 0.26 으로 꺾이고, 1.50 mm 부터는 대부분
Rayleigh 문턱 아래다.

**DIGIT 360 기준(MTF ≥ 0.5)으로는 204 행 중 14 행만 통과**하고, 센서 단위로는 사실상
어느 간격도 "분해"가 아니다. 같은 데이터에서 Rayleigh 와 MTF 가 **정반대의 헤드라인**을
준다. 어느 쪽을 쓰느냐는 선택이며, 논문에는 둘 다 실어야 한다.

### 4.2 깊이별

| 깊이 | 분해 행 | dip 중앙값 |
|---|---|---|
| 0.1 mm | 6 / 68 | 0.408 |
| 0.2 mm | 15 / 68 | 0.260 |
| 0.3 mm | 21 / 68 | 0.233 |

0.1 ~ 0.3 mm 안에서는 깊을수록 잘 갈라진다 — 자국이 진해져 신호 관문을 넘기 때문이다
(0.1 mm 에서는 dip 이 크지만 봉우리가 2.5 level 을 못 넘겨 대부분 **신호부족**).

> **2026-09-11 정정: 그 추세는 0.3 mm 까지만이다.** 위 표는 사다리가 0.3 mm 에서
> 멈춘 자료이고, "더 깊이 누르면 더 좁은 간격도 갈라질 가능성" 이라고 적어 둔 것은
> **틀렸다.** `pair010` pass 를 0.9 mm 까지 돌려 보니 골이 무너진다:
>
> | 깊이 | 분해 | dip 중앙 | 자국 중앙 |
> |---|---|---:|---:|
> | 0.1 mm | 2 / 8 | **0.391** | 2.99 |
> | 0.2 mm | 1 / 8 | 0.178 | 4.44 |
> | 0.3 mm | 0 / 8 | 0.121 | 7.96 |
> | 0.6 mm | 0 / 8 | 0.044 | 16.00 |
> | 0.9 mm | 0 / 5 | 0.054 | 26.30 |
>
> 자국은 계속 진해지는데(2.99 → 26.30 레벨) **골은 사라진다**(0.391 → 0.05). 두 자국이
> 커지며 서로 합쳐지기 때문이다. 그래서 두 점 분해능에는 **최적 깊이**가 있다: 자국이
> 판정 바닥을 넘을 만큼은 깊고, 두 자국이 합쳐지지 않을 만큼은 얕은 구간. 이 자료에서
> 그 구간은 **0.1 ~ 0.2 mm** 이고, 하필 판정이 가장 불안한 곳이다.

### 4.3 젤 두께 × 경도 — 함께 본다

두께와 경도는 따로 평균 낼 변수가 아니다(같은 이유를 `shape_reconstruction.md` §3.5
에 적었다). 3×3 격자의 모든 칸을 그대로 보이고, 그림은 x 경도, y 두께, z 지표의 3차원
막대다(칸마다 r1 왼쪽, r2 오른쪽).

![resolution: gaps resolved](figures/res3d_nresolved.png)
![resolution: dip at 1.75 mm](figures/res3d_dip175.png)

**분해한 간격 수 (0–4)** (칸마다 r1, r2 순)

| 두께 \ 경도 | soft | medium | hard |
|---|---|---|---|
| 1 mm | 3, 2 | 3, 0 | 3, 0 |
| 2 mm | 2, 3 | 3 | 4, 3 |
| 3 mm | 1, 2 | 0, 1 | 0, 0 |

**1.75 mm 에서의 최대 dip** (칸마다 r1, r2 순)

| 두께 \ 경도 | soft | medium | hard |
|---|---|---|---|
| 1 mm | 0.58, 0.53 | 0.20, 0.45 | 0.46, 0.70 |
| 2 mm | 0.37, 0.52 | 0.30 | 0.41, 0.29 |
| 3 mm | 0.23, 0.39 | 0.72, 0.62 | 0.58, 0.62 |

**접촉 대칭성 sym 중앙값** (칸마다 r1, r2 순)

| 두께 \ 경도 | soft | medium | hard |
|---|---|---|---|
| 1 mm | 0.84, 0.72 | 0.70, 0.53 | 0.73, 0.77 |
| 2 mm | 0.85, 0.90 | 0.91 | 0.74, 0.89 |
| 3 mm | 0.60, 0.74 | 0.57, 0.74 | 0.65, 0.62 |

읽는 법:

- **2 mm 행이 세 경도 모두에서 가장 많이 가른다** (soft 2·3, medium 3, hard 4·3 —
  2 mm 다섯 유닛이 15/20). 1 mm 행은 11/24, 3 mm 행은 4/24 이다.
  (2026-09-08 에 17 유닛 모두 실측 축척으로 바뀌고 pair100 두 건을 재측정했다. 칸 값은
  움직였지만 행의 순서는 그대로다: 2 mm > 1 mm > 3 mm.)
- 그러나 **2 mm 행은 접촉 대칭성도 가장 좋다** (sym 0.89 대 0.73 / 0.63). 두 기둥이 같은
  깊이로 들어간 정도가 두께 행과 함께 움직이므로, 이 격자에서 "2 mm 가 분해능이 좋다"
  와 "2 mm 가 잘 안착됐다" 를 가를 수 없다. sym 을 공변량으로 넣는 분석이 먼저다(미완).
- 같은 두께 행 안에서 경도를 따라가면 일관된 경향이 없다 — hard 2 mm(4, 3)가 가장 좋지만
  hard 1 mm(0, 1)와 hard 3 mm(1, 0)는 가장 나쁜 축에 든다. 경도 단독 효과는 이 데이터에서
  복제 간 산포보다 작다.
- 경도와 두께를 하나의 변수로 묶는 시도는 `contact_variable.md`.

### 4.4 센서별 패턴 — 다섯 간격 전부

칸은 **dip 값 + 깊이 + 판정**을 함께 보인다.

- **○** 0.58 **@0.2·0.3** — 세 관문(dip ≥ 0.265, 자국 ≥ 2.5 레벨, 잡음의 3 배)을 모두
  넘긴 깊이가 있었다. `@` 뒤는 **분해된 깊이 전부**(mm), dip 은 그 깊이들 중 최대다.
- **×** 0.20 **(0.1)** — 판정할 수 있었고 분해되지 않았다. 괄호는 **가장 큰 dip 이 난
  깊이**이고, 그 dip 이 앞의 숫자다.
- **△** — 어느 깊이에서도 자국이 2.5 레벨에 못 미쳐 **판정 자체가 불가능**했다.
- **—** 그 간격을 재지 않았다 (`pair010` 은 일부 유닛에만, §4.5).
- 마지막 열은 그 유닛의 자국 깊이 중앙값, ⚠ 는 판정 바닥보다 낮다는 뜻이다.
- 유닛 이름의 💡 는 **운전자가 빛이 새는 것을 육안으로 확인**한 유닛이다(§4.6).

`dip` 은 `1 − 가운데 골 / 양쪽 봉우리의 평균` 이다. 0 이면 두 자국이 한 덩어리로
보이고, 1 이면 골이 바닥까지 내려간다. 0.265 는 Rayleigh 의 고전적 한계다.
**추정기가 골을 부풀린다는 점이 중요하다** — 봉우리는 창 안의 최대, 골은 최소로 잡으므로
잡음이 양쪽을 반대로 밀고, 자국이 옅을수록 심하다. 자국 관문이 그것을 막으려고 있다.

| 유닛 | 두께 | 경도 | 1.10 mm | 1.25 mm | 1.50 mm | 1.75 mm | 2.00 mm | 분해 수 | 가장 좁은 분해 | 자국 peak 중앙 |
|---|---|---|---|---|---|---|---|---|---|---|
| soft_1mm_r1 | 1 mm | soft | × 0.12 (0.3) | × 0.20 (0.1) | **○** 0.27 @0.3 | **○** 0.58 @0.2·0.3 | **○** 0.49 @0.3 | 3 / 5 | 1.50 mm | 4.86 |
| soft_1mm_r2 | 1 mm | soft | × 0.45 (0.1) | × 0.56 (0.1) | × 0.61 (0.2) | **○** 0.50 @0.2·0.3 | **○** 0.47 @0.3 | 2 / 5 | 1.75 mm | 3.73 |
| soft_2mm_r1 | 2 mm | soft | × 0.26 (0.2) | × 0.17 (0.1) | × 0.61 (0.1) | **○** 0.37 @0.2·0.3 | **○** 0.49 @0.1·0.2·0.3 | 2 / 5 | 1.75 mm | 6.49 |
| soft_2mm_r2 | 2 mm | soft | × 0.67 (0.1) | **○** 0.52 @0.2 | × 0.26 (0.1) | **○** 0.33 @0.3 | **○** 0.55 @0.1·0.2·0.3 | 3 / 5 | 1.25 mm | 7.79 |
| soft_3mm_r1 | 3 mm | soft | — | × 0.60 (0.1) | △ 0.74 (0.1) | × 0.23 (0.3) | **○** 0.29 @0.3 | 1 / 4 | 2.00 mm | 1.75 ⚠ |
| soft_3mm_r2 | 3 mm | soft | × 0.48 (0.1) | **○** 0.27 @0.2 | × 0.41 (0.1) | × 0.39 (0.1) | **○** 0.45 @0.2·0.3 | 2 / 5 | 1.25 mm | 5.42 |
| medium_1mm_r1 | 1 mm | medium | **○** 0.49 @0.1·0.2 | **○** 0.45 @0.1 | **○** 0.31 @0.1 | × 0.20 (0.3) | **○** 0.50 @0.2·0.3 | 4 / 5 | 1.10 mm | 5.77 |
| medium_1mm_r2 💡 | 1 mm | medium | **○** 0.27 @0.1 | × 0.29 (0.3) | × 0.36 (0.3) | △ 0.45 (0.3) | △ 0.55 (0.3) | 1 / 5 | 1.10 mm | 1.83 ⚠ |
| medium_2mm_r2 | 2 mm | medium | **○** 0.32 @0.1 | **○** 0.44 @0.1 | × 0.25 (0.3) | **○** 0.30 @0.2 | **○** 0.48 @0.2·0.3 | 4 / 5 | 1.10 mm | 8.00 |
| medium_3mm_r1 | 3 mm | medium | × 0.71 (0.1) | △ 0.44 (0.1) | △ 0.30 (0.2) | △ 0.70 (0.1) | △ 0.24 (0.1) | 0 / 5 | — | 1.70 ⚠ |
| medium_3mm_r2 | 3 mm | medium | × 0.50 (0.1) | × 0.70 (0.1) | × 0.67 (0.1) | **○** 0.29 @0.3 | × 0.38 (0.2) | 1 / 5 | 1.75 mm | 3.36 |
| hard_1mm_r1 | 1 mm | hard | × 0.12 (0.1) | **○** 0.29 @0.1 | × 0.30 (0.1) | **○** 0.46 @0.1·0.3 | **○** 0.52 @0.2·0.3 | 3 / 5 | 1.25 mm | 5.71 |
| hard_1mm_r2 💡 | 1 mm | hard | **○** 0.30 @0.3 | × 0.69 (0.1) | △ 0.76 (0.2) | × 0.70 (0.1) | × 0.69 (0.1) | 1 / 5 | 1.10 mm | 2.03 ⚠ |
| hard_2mm_r1 | 2 mm | hard | × 0.46 (0.1) | **○** 0.39 @0.1·0.2 | **○** 0.41 @0.2·0.3 | **○** 0.34 @0.3 | **○** 0.62 @0.3 | 4 / 5 | 1.25 mm | 4.66 |
| hard_2mm_r2 | 2 mm | hard | × 0.59 (0.1) | × 0.57 (0.1) | **○** 0.27 @0.3 | **○** 0.29 @0.3 | **○** 0.59 @0.2·0.3 | 3 / 5 | 1.50 mm | 6.37 |
| hard_3mm_r1 | 3 mm | hard | × 0.44 (0.1) | × 0.45 (0.1) | △ 0.58 (0.1) | × 0.58 (0.1) | △ 0.70 (0.1) | 0 / 5 | — | 2.32 ⚠ |
| hard_3mm_r2 | 3 mm | hard | × 0.69 (0.1) | × 0.55 (0.1) | △ 0.59 (0.1) | × 0.62 (0.1) | △ 0.62 (0.1) | 0 / 5 | — | 1.91 ⚠ |
| **합계** | | | **4 / 16** | **6 / 17** | **4 / 17** | **9 / 17** | **11 / 17** | | | |

**분해는 위쪽 0.3 mm 안에서만 일어난다.** 279 단 중 분해로 인정된 49 단이 전부
0.1 · 0.2 · 0.3 mm 에 있고(11 / 16 / 22), **0.4 mm 이상에서 분해된 단은 하나도 없다** —
`pair010` 으로 0.9 mm 까지 눌러 본 뒤에도 그렇다. 더 깊이 누르면 자국은 진해지지만
두 자국이 서로 합쳐져 골이 사라진다(§4.2 의 정정).

**⚠ 표시가 결과를 지배한다.****⚠ 표시가 결과를 지배한다.** 자국 중앙값이 2.5 레벨 아래인 일곱 유닛은 분해 수가
0 ~ 1 이고, 2.5 위인 열 유닛은 2 ~ 4 다. **dip 자체는 두 무리가 다르지 않다** — ⚠ 무리의
최대 dip 은 0.44 ~ 0.76 으로 오히려 더 크다. 즉 이 표의 ○/× 는 **겔이 두 점을 얼마나
가르는가**보다 **자국이 얼마나 진한가**를 더 많이 말하고 있다(§4.6).

`medium_1mm_r2` 가 그 극단이다. 네 넓은 간격에서 전부 판정 불가·미분해인데
**가장 좁은 1.10 mm 는 분해한다**(dip 0.62). 물리적으로 불가능한 순서이므로, 이 유닛의
행은 분해능이 아니라 광학 상태를 읽고 있는 것이다 — 운전자가 이 유닛에서 빛이 새는
것을 확인했다(§4.6).

넓은 간격을 못 갈랐는데 좁은 간격을 가른 **역전**은 그 밖에도 여럿이다
(`soft_1mm_r1` 의 × × ○ ○ ○, `medium_3mm_r2` 의 × × ○ × 등). 물리적으로 넓은 간격이
더 어려울 수는 없으므로 이건 **측정 산포가 관문 근처에서 판정을 뒤집은 것**이다.
이 표를 "이 센서는 1.25 mm 를 분해한다" 로 읽으면 안 되고, §4.1 의 집계와 §4.6 의
경고를 함께 읽어야 한다. 두 건(§6)은 원인이 분명하다.

---

### 4.5 `pair010` (중심간격 1.10 mm) — 2026-09-11

1.25 mm 를 이미 분해해 한계가 **가장 좁은 프로브 아래로 미결**이던 6 유닛과, 이 프로브를
9DTact 바디에 처음 쓰는 것이라 확인용으로 넣은 2 유닛, 모두 8 유닛에 돌렸다.

| 유닛 | 1.10 mm | 분해된 깊이 | 한계 |
|---|---|---|---|
| `hard_1mm_r1` | 미분해 (0.123) | — | **(1.10, 1.25]** |
| `hard_2mm_r1` | 미분해 (0.458, 자국 얕음) | — | **(1.10, 1.25]** |
| `soft_2mm_r2` | 판정불가 (0.669, 자국 2.43 레벨) | — | **(1.10, 1.25]** |
| `soft_3mm_r2` | 판정불가 (0.478, 자국 얕음) | — | **(1.10, 1.25]** |
| `medium_1mm_r1` | **분해** (0.490) | 0.10 · 0.20 mm | < 1.10 — 미결 |
| `medium_2mm_r2` | **분해** (0.324) | 0.10 mm | < 1.10 — 미결 |
| `soft_1mm_r1` | 미분해 (0.116) | — | (1.25, 1.50] |
| `soft_2mm_r1` | 미분해 (0.263) | — | (1.50, 1.75] |

**한계가 두 간격 사이에 갇힌 유닛이 6 → 10 / 17 이 됐다.** 남은 7 중 2 는 1.10 mm 아래로
열려 있고(더 좁은 프로브가 필요하다), 5 는 2.00 mm 도 분해하지 못한다 — 그 5 에 대해서는
아래를 보라.

`soft_2mm_r2` 의 0.10 mm 단은 dip **0.669** 로 Digit 360 기준(0.667)까지 넘지만 자국이
2.43 레벨이라 판정되지 않았다. 가장 좁은 간격에서 가장 큰 골이 가장 옅은 자국에 실린다.

### 4.6 "아무것도 분해 못 한" 5 유닛은 분해능이 아니라 **자국 대비**의 문제다

2.00 mm 조차 분해하지 못한 유닛이 5 개 있다(`hard_1mm_r2`, `hard_3mm_r1`, `hard_3mm_r2`,
`medium_1mm_r2`, `medium_3mm_r1`). 그 이유를 보면 **골이 얕아서가 아니다.**

| | 자국 깊이 peak (중앙) | 최대 dip |
|---|---:|---:|
| 아무것도 분해 못 한 5 유닛 | **1.73 레벨** | 0.55 – 0.76 |
| 나머지 12 유닛 | **5.16 레벨** | 0.48 – 0.67 |
| | Mann-Whitney **p 0.0023** | |

**그 다섯의 골은 오히려 평균보다 깊다.** 두 기둥은 갈라져 보이는데 자국이 `MIN_PEAK`
2.5 레벨에 못 미쳐 인정되지 않은 것이다. 결정적인 것은 같은 겔 규격의 짝이다 —
`hard_1mm_r1` 의 자국이 5.71 레벨, `hard_1mm_r2` 가 **1.72 레벨**로 3.3 배 차이다.
겔 설계로는 설명되지 않고 **바디·조립**을 가리킨다.

**그 다섯 중 둘은 운전자가 빛이 새는 것을 육안으로 확인했다**(2026-09-11):
`9DTact_medium_1mm_r2` 와 `9DTact_hard_1mm_r2`. 등록부에 `suspect_hardware: true` 로
적었다. 새는 빛은 배경을 밝혀 자국의 대비를 떨어뜨리므로 증상이 정확히 들어맞는다.

**확인된 둘 중 하나가 위의 결정적 짝의 옅은 쪽이다.** `hard_1mm_r1`(5.71 레벨) 대
`hard_1mm_r2`(2.03 레벨)의 차이를 "겔이 아니라 바디" 의 증거로 들었는데, 그 옅은
쪽에서 실제로 빛이 샌다. 추론이 관찰로 닫혔다.

**그리고 확인된 두 유닛이 똑같이 불가능한 순서를 보인다** — 넓은 네 간격에서는
아무것도 분해하지 못하면서 **가장 좁은 1.10 mm 만 분해한다.**

| 유닛 | 빛 샘 | 1.25 ~ 2.00 mm | 1.10 mm |
|---|---|---|---|
| `medium_1mm_r2` | 확인 | 전부 실패 | **분해** (dip 0.62, 0.1 mm) |
| `hard_1mm_r2` | 확인 | 전부 실패 | **분해** (dip 0.30, 0.3 mm) |

넓은 간격이 좁은 간격보다 어려울 수는 없다. 두 유닛의 행은 **분해능이 아니라 광학
상태**를 읽고 있었고, 그 원인이 관찰로 확인됐다. 두 유닛 모두 1 mm 겔이라는 점도
같다 — 얇은 겔일수록 새는 빛이 자국 대비를 더 크게 깎는다는 뜻일 수 있다.

**따라서 §4.3 의 두께 × 경도 격자와 §4.4 의 센서별 패턴은 이 다섯 유닛에 대해 겔의
성질을 말하지 않는다.** 그 칸들은 광학·조립 상태를 말하고 있을 가능성이 크다. 나머지
12 유닛만으로 다시 보거나, 그 다섯의 자국 대비를 먼저 고쳐야 한다.

### 4.7 `pair150` (중심간격 2.50 mm) — 측정이 작동하는 유일한 간격

2026-09-11, **16 / 16 유닛**(측정 가능한 전부, 파괴된 둘 제외). 넓은 간격을 재는 이유는
한계를 좁히려는 것이 아니다 — §4.6 이 남긴 질문, **"2.00 mm 조차 분해하지 못한 유닛들의
실패가 분해능인가 자국 대비인가"** 에 답하기 위해서다. 두 자국이 겹칠 수 없는 거리에서
못 가른다면 분해능이 아니다.

| 유닛 | 경도 | 두께 | 자국이 바닥 넘는 깊이 | Rayleigh 분해 | **D360 분해** | 최대 dip |
|---|---|---|---:|---:|---:|---:|
| `soft_1mm_r1` | soft | 1 mm | 0.3 mm | 4 / 6 | **4** | 0.838 |
| `soft_1mm_r2` | soft | 1 mm | 0.1 mm | 6 / 6 | **4** | 0.840 |
| `medium_1mm_r1` | medium | 1 mm | 0.1 mm | 5 / 6 | **1** | 0.693 |
| `medium_1mm_r2` 💡 | medium | 1 mm | — | 0 / 6 | **0** | 0.593 |
| `hard_1mm_r1` | hard | 1 mm | 0.3 mm | 3 / 6 | **1** | 0.690 |
| `hard_1mm_r2` 💡 | hard | 1 mm | 0.2 mm | 5 / 6 | **0** | 0.418 |
| `soft_2mm_r1` | soft | 2 mm | 0.1 mm | 9 / 9 | **7** | 0.707 |
| `soft_2mm_r2` | soft | 2 mm | 0.1 mm | 9 / 9 | **3** | 0.698 |
| `medium_2mm_r2` | medium | 2 mm | 0.1 mm | 9 / 9 | **4** | 0.728 |
| `hard_2mm_r1` | hard | 2 mm | 0.2 mm | 8 / 9 | **2** | 0.676 |
| `hard_2mm_r2` | hard | 2 mm | 0.2 mm | 7 / 9 | **0** | 0.593 |
| `soft_3mm_r2` | soft | 3 mm | 0.6 mm | 4 / 9 | **0** | 0.715 |
| `medium_3mm_r1` | medium | 3 mm | 0.5 mm | 5 / 9 | **0** | 0.656 |
| `medium_3mm_r2` | medium | 3 mm | 0.4 mm | 6 / 9 | **0** | 0.676 |
| `hard_3mm_r1` | hard | 3 mm | 0.4 mm | 6 / 9 | **0** | 0.628 |
| `hard_3mm_r2` | hard | 3 mm | 0.3 mm | 7 / 9 | **0** | 0.593 |

#### 답 1 — 다섯 유닛 중 넷은 멀쩡했다. 사다리가 얕았을 뿐이다

§4.6 이 "바디·조립" 을 가리킨다고 적었던 다섯 유닛 중 **넷이 2.50 mm 에서 깨끗하게
분해한다.** 이유는 단순하다: 그 유닛들은 자국이 판정 바닥(2.5 레벨)을 넘는 데
0.3 ~ 0.5 mm 가 필요한데, **옛 pass 의 사다리가 0.3 mm 에서 끝났다.** 누를 기회가
없었던 것이다. 진단을 정정한다 — **원인은 바디가 아니라 측정 범위였다.**

남은 하나, `medium_1mm_r2` 는 다르다. 2.50 mm 에서도 **여섯 단 전부 판정 불가**이고
자국이 깊이와 함께 진해지지 않는다(2.35 → 1.74 레벨). 같은 칸의 짝 `medium_1mm_r1` 은
첫 단부터 통과한다. **이 유닛만 실제 불량이다.**

#### 답 2 — "0.4 mm 보다 깊으면 분해되지 않는다" 는 좁은 간격의 사실이었다

`spatial_resolution_sensitivity.md` 가 "깊어지면 두 자국이 합쳐져 골이 없다" 고 결론했다.
2.50 mm 에서는 **0.9 mm 까지 분해가 계속된다** — 여덟 유닛에서 그렇다. 충분히 멀면
합쳐지지 않는다. 그 문서의 결론은 **1.10 ~ 2.00 mm 에 한정해서** 읽어야 한다.

#### 답 3 — 겔 의존성이 여기서 처음 깨끗하게 보인다

건강한 14 유닛(빛 샘 둘 제외):

| 관계 | ρ | p |
|---|---:|---:|
| **경도 → 최대 dip** | **−0.82** | **0.0004** |
| 두께 → D360 분해 단수 | −0.69 | 0.0068 |
| 두께 → 자국이 바닥 넘는 깊이 | +0.65 | 0.0127 |
| 두께 → 최대 dip | −0.54 | 0.0449 |
| 경도 → D360 분해 단수 | −0.54 | 0.0473 |
| 경도 → 자국이 바닥 넘는 깊이 | +0.23 | 0.44 (없음) |

**무른 겔이 두 점을 더 깊은 골로 가르고, 두꺼운 겔은 모든 지표에서 나쁘다.**
Digit 360 기준을 통과한 단은 **1 ~ 2 mm 겔에만** 있고 3 mm 다섯 유닛은 전부 0 이다.

**이것이 2.50 mm 에서만 보이는 이유**가 이 절의 핵심이다. 좁은 간격에서는 자국 대비
관문이 얕은 단을 쳐내고 합쳐짐이 깊은 단을 쳐내서, 남는 칸이 유닛마다 달랐다 — 그래서
§4.4 의 ○/× 는 겔의 성질보다 측정의 성질을 더 많이 담았다. 2.50 mm 에서는 0.1 ~ 0.9 mm
전 구간이 측정되므로 **같은 잣대로 겔을 비교할 수 있다.**

---

## 5. 판정 기준의 민감도 — 결과의 일부로 보고할 것

`MIN_PEAK` 를 2.5 에서 2.0 회색조로 내리면 **1.25 mm 의 분해 센서가 5 → 9** 로 뛴다.
0.5 level 은 0.01 mm 에 해당하는 차이다. 즉 좁은 간격의 판정은 신호 관문에 걸쳐 있고,
문턱을 조금 움직이면 답이 크게 바뀐다. 이 민감도는 조정해서 없앨 것이 아니라
**한계로 보고**한다. Rayleigh 0.265 는 광학의 관례이고, 2.5 level 은 §3 의 근거로
고정했다.

---

## 6. 두 기둥은 왜 같은 깊이로 안 들어가는가

같은 유닛(`soft_3mm_r1`)에 같은 프로브(pair100)로 두 번 재되, 사이에 **툴을 프로브
축 둘레로 180° 돌렸다.** 왼쪽/오른쪽 봉우리 비의 로그를 S(젤 쪽 원인) + P(프로브 쪽
원인)으로 놓으면 0° 에서 S + P, 180° 에서 S − P 가 읽히므로 둘이 분리된다:

| 원인 | log(좌/우) | 해석 |
|---|---|---|
| **S — 젤** | **−0.440 ± 0.060** | 젤 평면 기울기, 유닛의 속성 |
| P — 프로브 | +0.131 ± 0.029 | 두 기둥의 길이 차, 프로브의 속성 |

**젤이 프로브보다 3.4배 크다.** 그래서 정렬을 실측 젤 법선에 맞추는 것(§2-1)이 이
시험의 전제다. 자세한 것은 `pair_contact_asymmetry.md`. (그 회전 시험에서 Cartesian
자세 명령으로 툴을 돌린 것은 잘못된 방법이었다 — IK 가 다른 가지를 택해 팔이 617 mm
로 튀었다. 조인트 6 만 돌려야 한다.)

**밀려 있던 두 건은 2026-09-08 에 재측정했고, 원인은 짐작과 달랐다.**
`soft_1mm_r1` 과 `hard_1mm_r1` 의 pair100(2.00 mm) 약한 봉우리가 1.22 / 0.96 level 로,
같은 유닛의 다른 세 프로브(3.90–7.06)와 pair100 코호트 중앙값(3.95)의 1/4 이었다.
접촉 면적이 같은 프로브에서 나올 수 없는 값이고, 그 결과 soft_1mm_r1 은 2.00 mm 는
못 가르고 1.75/1.50 은 가르는 불가능한 순서가 됐다.

원래 의심은 **접근 여백**이었다 — 두 런 모두 패스 초기라 여백이 1.0 mm 였고, 팁이
1.10 mm 길어 시작점이 이미 접촉면에 걸쳐 깊이 기준이 흔들렸다는 것. **사다리가 이를
기각한다.** 0.3 mm 단에서 17 유닛 전부가 0.266–0.290 mm 에 도달했고 두 문제 런도
0.278 mm 로 코호트 한가운데였다. 힘(0.13–0.19 N)도 코호트 범위(0.09–0.37 N) 안이고,
약한 봉우리는 패스 전반에 흩어져 있었지 앞쪽에 몰려 있지 않았다.

재측정은 **깊이가 아니라 접촉이 없었음**을 보인다. 같은 깊이에서 자국 면적이:

| 목표 깊이 | soft_1mm_r1 원본 → 재측정 | hard_1mm_r1 원본 → 재측정 |
|---|---|---|
| 0.1 mm | 0 → 4,219 px | 0 → 397 px |
| 0.2 mm | 0 → 12,110 px | 0 → 9,565 px |
| 0.3 mm | 564 → 33,539 px | 553 → 17,292 px |

깊이는 같은데(0.278 대 0.286 mm) 자국만 60 배 차이다. 힘은 원본에서도 0.13–0.19 N 이
나왔으니 닿기는 닿았다 — 즉 **기울어져 모서리만 닿은 것**이고, 깊이 기준이 아니라
장착 시의 자세·정렬 문제다. 젤 평면 기울기가 유닛마다 다르고(§4 `campaign_protocol.md`)
두 기둥이 2 mm 떨어져 있으므로, 평면에서 조금만 벗어나도 한쪽 기둥만 눌린다.

재측정 결과 **2.00 mm 는 9 → 11/17**, soft_1mm_r1 은 2/4 → 3/4, hard_1mm_r1 은
0/4 → 3/4(0.20 mm 단에서도 분해). 전체 분해 rung 은 42 → 45.

**남은 위험:** 이 실패 방식은 조용하다 — 로그에 오류가 없고 깊이도 힘도 정상으로
보인다. 자국 면적이 코호트에서 벗어나는지를 런 직후에 확인하는 검사가 없다.

---

## 7. 한계와 주의

- **축척은 이제 17 유닛 모두 실측이다** (2026-09-08, `campaign_protocol.md` §4). 중앙값
  대체는 더 이상 쓰이지 않는다. 그 중 8 유닛은 자기 ball4 런이 아니라 같은 유닛의 다른
  pass(cube4/cyl4)에서 빌린 값이다 — 축척은 광학의 성질이라 프로브와 무관하므로 유효하다.
  두 프로브 이상으로 잰 9 유닛에서 kx 가 대개 1.5–4.5 % 안에서 맞고, 두 유닛만 12.5 %
  차이가 난다. 그 두 유닛의 lp/mm 에는 그만큼의 불확실성이 붙는다. dip 자체는 축의
  늘림에 둔감하다.
- **판정은 이미지 한 장의 프로파일**이다. 깊이당 1회 반복이라 깊이 안에서의 산포는
  없고, 산포는 깊이 사이의 일관성(§4.4 의 역전)으로만 드러난다.
- **0.3 mm 까지만** 눌렀다(§4.2).
- 두께 효과는 안착과 뒤섞여 있다(§4.3).
- `resolution_measurements.csv` 는 (패스·센서)당 정확히 하나의 런에서 왔다. 2026-09-07
  정리 때 `soft_3mm_r1` 2.00 mm 의 폐기된 런 행 6 개를 지웠고(210 → 204), 남은 204 행은
  `analyse_resolution.py` 로 소수점까지 재현됨을 확인했다.

---

## 8. 재현

```bash
/usr/bin/python3 src/scripts/analyse_resolution.py 20260905_passA_pair050 9DTact_hard_3mm_r2
```

문턱값은 스크립트 머리의 `RAYLEIGH`, `MTF_MIN`, `MIN_PEAK`, `NOISE_K`. 런 선택 규칙과
축척 대체 규칙은 같은 파일의 `main()` 과 `jacobian()`.
