# VBTS 해상도 캠페인 — 작업 규칙

저장소가 무엇인지는 `README.md` 에 있다. 이 파일은 **작업할 때 지켜야 하는 것**만 적는다.

## 1. 분석 집합 — 칸마다 정해진 센서 하나만 쓴다

**운전자 결정 (2026-09-14). 이 규칙이 다른 모든 편의보다 앞선다.**

겔은 경도 3 × 두께 3 의 아홉 칸이고 칸마다 복제를 둘씩 만들었지만(원리당 18,
합 54), **분석과 논문 그림은 칸마다 고른 시편 하나만 쓴다** — 원리당 9, 합 27.

- 고르는 것은 `src/scripts/result_common.py` 의 `chosen(pr)` 다.
  규칙: 확정 불량(빛 누출) → 독립 증거 → 파괴 → 그 밖에는 `r1`.
- 자료는 `result/single/` 아래에 있다. 문서는 `result/results_single_sensor.md`.
- 스크립트는 환경변수 `VBTS_SINGLE=1` 로 이 집합을 고른다.

**하지 말 것**
- 54 유닛 집합(`result/`, `results.md`)의 값을 논문 그림이나 본문에 섞지 않는다.
  같은 양이 두 값으로 두 번 나온다 — 실제로 이미 그렇게 되어 있다.
- 고르지 않은 복제를 "산포를 보여주려고" 그림에 겹쳐 찍지 않는다.
- 두 집합의 숫자를 한 문장 안에서 인용하지 않는다.

**예외는 하나뿐이다.** 제작 간 반복성(복제 산포)은 정의상 54 유닛 집합에서만
나온다. 이것은 **한계 절의 숫자로만** 인용하고, 그림으로 그리지 않는다.
인용할 때는 54 유닛 집합에서 나온 값임을 함께 적는다.

## 2. 논문 그림

- **그림도 코드도 `paper/fin_figures/` 에 있다** (운전자 결정, 2026-09-14).
  그 폴더만 건네면 누구든 다시 그린다. 돌리는 법은 그 폴더의 `README.md`.
- 판형·색·저장은 `paper/fin_figures/paper_style.py` 한 곳에서 건다.
  새 그림에서 rcParams 를 다시 쓰지 않는다.
- IEEE 두 단 판형: 한 단 3.50 in · 두 단 7.16 in.
  **두 단 그림은 7.16 보다 좁게 그려도 된다** (운전자 결정, 2026-09-15). 본문에
  `\textwidth` 로 앉히면 그만큼 확대되므로 글자도 함께 커진다 — 좁게 그리고 크게
  앉히는 것이 작은 글자를 키우는 가장 싼 방법이다. 패널 비율이 뭉개지지 않는
  선까지만 줄인다. 두 패널짜리 기본값은 `paper_style.FULL_W_NARROW`.
- 글자 크기는 **인쇄에서 읽히는 것**이 기준이다. `paper_style` 한 곳에서 건다.
- **논문 그림의 색은 `paper_style.py` 가 정한다** (운전자 결정, 2026-09-15).
  순수 원색 파랑 `#0000FF` · 초록 `#00FF00` · 빨강 `#FF0000` · 검정 `#000000` 넷뿐이고,
  순서가 있는 변수는 무를수록 파랑 · 단단할수록 빨강으로 간다.
  `result/` 의 그림은 `src/scripts/palette.py` 의 Okabe-Ito 셋을 그대로 쓴다 —
  **두 곳이 갈린다.** 값을 옮겨 적지 말고 각자의 파일에서 가져올 것.
  > **순수 초록이 흰 바탕에서 거의 안 보인다** — `#00FF00` 의 바탕 대비가
  > **1.34 : 1** 로 검증기 기준 3 : 1 아래다. 화면에서는 보이지만 종이에 뽑으면
  > 가는 초록 선이 사라진다. 색을 바꾸지 않고 살리려면 선을 굵게 하거나 초록에만
  > 흰 테두리를 두르면 된다.
  > 색각 이상 쪽은 오히려 낫다 — 빨강↔초록 ΔE **22.2** (deutan), 보통 시야 52.0.
  > 밝기가 크게 달라 색을 못 보는 눈도 가른다.
- 글꼴은 Type 42 로 심는다. Type 3 은 IEEE PDF eXpress 가 되돌려 보낸다.
- **csv 없이 그림을 저장하지 않는다.** `paper_style.save()` 가 pdf·png·csv 를 함께 낸다.
- `paper/figures/` 는 그 전에 만든 것이 들어 있는 곳이라 섞지 않는다.
- 색은 `src/scripts/palette.py` 하나에서만 나온다 — `result/` 의 그림과 같은 색을
  써야 하므로 값을 베껴 두지 않는다.
- 무엇을 싣고 무엇을 빼는가는 `paper/figure_plan.md` 가 정본이다.

## 3. 자료

- `data/` 는 저장소에 없다 — 실험 기계에만 있다.
- **`result/` 는 예외**라 그림과 그것을 그린 csv 가 함께 들어 있다.
  그래서 천장·분해능·요구 밀도처럼 csv 로 끝나는 그림은 어느 기계에서나 다시 그린다.
  원영상이 필요한 그림(자국 성장 곡선, 형상 높이맵)만 실험 기계에서 돌린다.

## 4. 주장의 상한

관찰한 경향을 적는 논문이고 가이드라인을 약속하지 않는다.

- "무관하다" 가 아니라 **"이 설계로는 검출되지 않았다"**.
- 평탄 밀도의 무연관은 독립성이 아니라 **검출 실패**로 적는다.
- 경도 효과를 말할 때는 흔든 폭을 함께 적는다 — 9DTact 40 Shore 점, 광도 계열 6 점.
